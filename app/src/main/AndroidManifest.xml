<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.relicon">

    <uses-permission android:name="android.permission.ACCEPT_HANDOVER" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Relicon.Fullscreen"
        android:versionCode="1000"
        android:versionName="1.0.0">
        <activity android:name=".CustomizationProection"></activity>
        <activity android:name=".BraceletReadSleep" />
        <activity android:name=".SleepControlMode" />
        <activity android:name=".ProectionMode" />
        <activity android:name=".ExtraActivity" />
        <activity android:name=".TodaySleepHours" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".SleepCountChanger" />
        <activity android:name=".TryingConnectionActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>